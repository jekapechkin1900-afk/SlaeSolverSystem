# Распределённая информационная система решения СЛАУ

Проект представляет собой программную систему на .NET/C# для распределенного решения систем линейных алгебраических уравнений (СЛАУ) в условиях локальной вычислительной сети (ЛВС). Система использует архитектуру "Master-Worker" и протокол TCP для обмена данными.

## 🚀 Особенности

*   **Распределенные вычисления:** Решение СЛАУ распараллеливается на несколько счётных узлов (Worker'ов) для ускорения вычислений.
*   **Два метода решения:**
    1.  **Распределенный метод Гаусса-Зейделя** (реализован как параллельный метод Якоби).
    2.  **Линейный (однопоточный) прямой метод Гаусса** для сравнения производительности.
*   **Современный UI:** Графический интерфейс на WPF с использованием библиотеки **MahApps.Metro** для чистого и отзывчивого дизайна.
*   **Сравнение производительности:** Встроенный инструмент для запуска тестов на одних и тех же данных и наглядного сравнения времени выполнения с автоматическим расчетом ускорения (Speedup).
*   **Надежная работа с файлами:** Поддержка Drag-and-Drop, корректная обработка различных кодировок (UTF-8 с BOM) и культурных форматов чисел.
*   **Отказоустойчивость:** Реализованы тайм-ауты на подключение Worker'ов и корректная обработка ошибок (неверные данные, потеря соединения).
*   **Тестовое покрытие:** Проект включает набор модульных (xUnit) и нагрузочных тестов для проверки корректности и производительности.

## 🛠️ Архитектура

Система построена на принципах ООП и разделения ответственности (Single Responsibility Principle).

1.  **`SlaeSolverSystem.Common`**: Общая библиотека классов, содержащая:
    *   Сетевой протокол (`CommandCodes.cs`).
    *   Контракты данных (`CalculationContracts.cs`).
    *   Реализацию математического ядра для прямого метода Гаусса (`LinearSlaeSolver.cs`).

2.  **`SlaeSolverSystem.Master`**: Master-сервер. Является координатором вычислений.
    *   `MasterApp`: Главный класс, оркестрирующий работу всех компонентов.
    *   `ConnectionManager`: Принимает сетевые подключения от клиентов.
    *   `WorkerPool`: Управляет пулом доступных Worker'ов.
    *   `GuiCommandHandler`: Обрабатывает команды, поступающие от WPF-клиента.
    *   `JobManager`: Управляет очередью и выполнением вычислительных заданий (`LinearJob`, `DistributedJob`).

3.  **`SlaeSolverSystem.Worker`**: Счётный узел (Worker).
    *   `WorkerApp`: Главный класс, управляющий жизненным циклом Worker'а.
    *   `MasterClient`: Инкапсулирует сетевое взаимодействие с Master'ом.
    *   `WorkerTask`: Содержит логику вычислений и состояние текущей подзадачи.
    *   `MessageHandler`: Обрабатывает входящие от Master'а команды.

4.  **`SlaeSolverSystem.Client.Wpf`**: WPF-приложение (GUI).
    *   Реализовано на паттерне **MVVM**.
    *   `Views`: XAML-разметка окон и страниц.
    *   `ViewModels`: Логика представления (`MainViewModel`).
    *   `Services`: Сетевой клиент `MasterApiClient` для взаимодействия с Master-сервером.

5.  **`SlaeSolverSystem.Tests`**: Тестовый проект (xUnit).
    *   **Модульные тесты:** Тестирование `LinearSlaeSolver`, `WorkerTask` и других классов в изоляции.
    *   **Нагрузочные и интеграционные тесты:** Параметризованные тесты для проверки всей системы на данных разного размера.

## ⚙️ Технологический стек

*   **Платформа:** .NET 9
*   **Язык:** C#
*   **UI:** WPF + MahApps.Metro
*   **Тестирование:** xUnit
*   **Сетевой протокол:** TCP/IP

## 🚀 Как запустить

### 1. Подготовка

*   **Среда:** Visual Studio 2022 с установленной рабочей нагрузкой ".NET Desktop Development".
*   **.NET SDK:** .NET 8.0 или выше.

### 2. Сборка проекта

1.  Откройте файл `SlaeSolverSystem.sln` в Visual Studio.
2.  Выполните **Сборка -> Пересобрать решение (Build -> Rebuild Solution)**.

### 3. Запуск системы

Для корректной работы нужно запустить как минимум три компонента: Master, один Worker и GUI.

1.  **Запуск Master-сервера:**
    *   Перейдите в папку `SlaeSolverSystem.Master/bin/Debug/net8.0/`.
    *   Запустите `SlaeSolverSystem.Master.exe`.
    *   Появится консольное окно с сообщениями о прослушивании портов. **Не закрывайте его.**

2.  **Запуск Worker'ов:**
    *   Перейдите в папку `SlaeSolverSystem.Worker/bin/Debug/net8.0/`.
    *   Запустите столько экземпляров `SlaeSolverSystem.Worker.exe`, сколько вам нужно для теста. Каждый Worker откроется в своем консольном окне.

3.  **Запуск GUI-клиента:**
    *   Перейдите в папку `SlaeSolverSystem.Client.Wpf/bin/Debug/net8.0-windows/`.
    *   Запустите `SlaeSolverSystem.Client.Wpf.exe`.
    *   Откроется главное окно приложения.

### 4. Работа с приложением

1.  **Подготовьте данные:** Используйте Python-скрипт `generate_data_500.py` для создания тестовых файлов `matrix_500.txt` и `vector_500.txt`.
2.  **Создайте файл узлов:** Создайте текстовый файл `nodes.txt` и впишите в него `127.0.0.1` столько раз, сколько Worker'ов вы запустили.
3.  **Выберите файлы в приложении:** На вкладке "Решение СЛАУ" выберите пути к трем созданным файлам, используя кнопки "Обзор..." или перетаскивая файлы на поля ввода.
4.  **Запустите тесты:** Перейдите на вкладку "Сравнение производительности" и запустите "Линейный тест", а затем "Распределенный тест", чтобы увидеть результаты в таблице.
5.  **Просмотрите логи:** На вкладке "Логи" можно отслеживать детальный ход выполнения операций.

## ✅ Тестирование

Для запуска тестов:
1.  Откройте **Вид -> Обозреватель тестов (View -> Test Explorer)** в Visual Studio.
2.  **Модульные тесты:** Можно запускать в любое время. Они не требуют запущенных компонентов системы.
3.  **Нагрузочные и интеграционные тесты (помечены трейтами "Load" и "Integration"):** Требуют, чтобы Master-сервер и необходимое количество Worker'ов были запущены.
